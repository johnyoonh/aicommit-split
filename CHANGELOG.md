# Changelog

## 0.1.0

- Initial public package for `aicommit-split`.
- Split working-tree changes into deterministic groups before invoking `aicommit`.
- Keep Python import-only hunks with the substantive hunks that need them.
